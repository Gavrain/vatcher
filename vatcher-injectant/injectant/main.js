import { deliverOnload, deliverBeforeunload } from "./utils/deliver"

(() => {
    const config = __vl.config
    const { target, pid } = config
    fetch(tar)

    deliverOnload(target, {})
    deliverBeforeunload(target, {})
})()